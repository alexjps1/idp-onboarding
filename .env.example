# Copy to .env.local and fill in a real key.
OPENAI_API_KEY=sk-your-key-here
OPENAI_TRANSCRIPTION_MODEL=gpt-4o-mini-transcribe
OPENAI_CONTENT_MODEL=gpt-4o-mini
OPENAI_REALTIME_MODEL=gpt-realtime
OPENAI_REALTIME_VOICE=cedar

# Set to "true" to type to the voice agent instead of speaking (no mic prompt).
# Leave unset/false for the real microphone (e.g. on the study iPad).
NEXT_PUBLIC_REALTIME_TEXT_INPUT=false

# host:port of the SILAB TCP server (FS-SCENERY). Used by /api/adas to poll the
# driving-automation (ADAS) state. Defaults to the value below if unset.
SILAB_ADDR=10.152.238.2:4200

# Subpath the app is served under behind the reverse proxy. Must match
# next.config.mjs's basePath — see lib/base-path.ts. Leave unset to serve at
# the domain root.
NEXT_PUBLIC_BASE_PATH=/idp-app
